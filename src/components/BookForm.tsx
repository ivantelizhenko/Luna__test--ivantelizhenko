import styled from 'styled-components';

const StyledBookForm = styled.form`
  height: 100%;
  background-color: #fefae0;
  padding: 1.2rem;
  border-radius: 4px;
`;

function BookForm() {
  return <StyledBookForm>Form</StyledBookForm>;
}

export default BookForm;
